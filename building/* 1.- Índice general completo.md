Correcto. Desde ahora seguimos con **secuencia numérica fija**, para que no se pierda nada.

# Índice general completo

## Fase 1 — Base de datos Supabase

**1.** Crear extensiones.
**2.** Crear ENUMS.
**3.** Crear tablas principales.
**4.** Crear variables narrativas.
**5.** Crear tablas de versiones, reviews, ranking y créditos.
**6.** Crear tabla `features`.
**7.** Crear tabla central `embeddings`.
**8.** Crear función SQL `match_embeddings`.
**9.** Crear tablas `ai_logs` y `partner_invitations`.
**10.** Crear índices.
**11.** Activar RLS.
**12.** Crear policies.
**13.** Crear Storage Buckets.
**14.** Crear trigger automático de profiles.

Esto ya te lo entregué en SQL.

---

## Fase 2 — Secrets en Supabase

**15.** Ir a Supabase Dashboard.
**16.** Ir a `Project Settings`.
**17.** Ir a `Edge Functions`.
**18.** Ir a `Secrets`.
**19.** Crear secret:

```text
GEMINI_API_KEY
```

---

## Fase 3 — Edge Functions

**20.** Crear Edge Function `create-embedding`.
**21.** Pegar código `create-embedding/index.ts`.
**22.** Probar `create-embedding`.

Ya te entregué el código de `create-embedding`.

Ahora seguimos con:

**23.** Crear Edge Function `semantic-search`.
**24.** Pegar código `semantic-search/index.ts`.
**25.** Probar `semantic-search`.

Después seguiremos con:

**26.** Crear Edge Function `generate-version`.
**27.** Pegar código `generate-version/index.ts`.
**28.** Probar generación de versión.

Luego:

**29.** Crear Edge Function `review-version`.
**30.** Pegar código `review-version/index.ts`.
**31.** Probar review de versión.

Luego:

**32.** Crear Edge Function `consume-credit`.
**33.** Pegar código `consume-credit/index.ts`.
**34.** Probar consumo de créditos.

---

## Fase 4 — Datos de prueba

**35.** Crear usuario de prueba.
**36.** Insertar libro de prueba.
**37.** Insertar capítulo de prueba.
**38.** Crear embedding del libro.
**39.** Crear embedding del capítulo.
**40.** Ejecutar búsqueda semántica.
**41.** Generar versión nueva.
**42.** Revisar versión con IA.
**43.** Confirmar que aparece en `story_versions`.
**44.** Confirmar que aparece en `ai_reviews`.
**45.** Confirmar que aparece en `embeddings`.

---

## Fase 5 — FrontEnd Claude

**46.** Entregar a Claude el esquema de Supabase.
**47.** Conectar Supabase Auth.
**48.** Crear Book Library.
**49.** Crear Chapter Reader.
**50.** Crear Variable Editor.
**51.** Crear Generate Version Screen.
**52.** Crear AI Review Panel.
**53.** Crear Semantic Search Page.
**54.** Crear Ranking Page.
**55.** Crear página MVP/BETA Roadmap.

---

# Ahora continuamos con el paso 23

## 23. Crear Edge Function `semantic-search`

En Supabase Dashboard:

```text
Edge Functions
→ Create a new function
```

Nombre:

```text
semantic-search
```

Luego abre:

```text
semantic-search
→ index.ts
```

En el próximo mensaje te doy el código completo para copy-paste:

```text
semantic-search/index.ts
```
