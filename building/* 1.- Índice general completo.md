Correcto. Desde ahora seguimos con **secuencia numérica fija**, para que no se pierda nada.

# Índice general completo

## Fase 1 — Base de datos Supabase

**1.** Crear extensiones.
**2.** Crear ENUMS.
**3.** Crear tablas principales.
**4.** Crear variables narrativas.
**5.** Crear tablas de versiones, reviews, ranking y créditos.
**6.** Crear tabla `features`.
**7.** Crear tabla central `embeddings`.
**8.** Crear función SQL `match_embeddings`.
**9.** Crear tablas `ai_logs` y `partner_invitations`.
**10.** Crear índices.
**11.** Activar RLS.
**12.** Crear policies.
**13.** Crear Storage Buckets.
**14.** Crear trigger automático de profiles.

Esto ya te lo entregué en SQL.

---

## Fase 2 — Secrets en Supabase

**15.** Ir a Supabase Dashboard.
**16.** Ir a `Project Settings`.
**17.** Ir a `Edge Functions`.
**18.** Ir a `Secrets`.
**19.** Crear secret:

```text
GEMINI_API_KEY
```

---

## Fase 3 — Edge Functions

**20.** Crear Edge Function `create-embedding`.
**21.** Pegar código `create-embedding/index.ts`.
**22.** Probar `create-embedding`.

Ya te entregué el código de `create-embedding`.

Ahora seguimos con:

**23.** Crear Edge Function `semantic-search`.
**24.** Pegar código `semantic-search/index.ts`.
**25.** Probar `semantic-search`.

Después seguiremos con:

**26.** Crear Edge Function `generate-version`.
**27.** Pegar código `generate-version/index.ts`.
**28.** Probar generación de versión.

Luego:

**29.** Crear Edge Function `review-version`.
**30.** Pegar código `review-version/index.ts`.
**31.** Probar review de versión.

Luego:

**32.** Crear Edge Function `consume-credit`.
**33.** Pegar código `consume-credit/index.ts`.
**34.** Probar consumo de créditos.

---

## Fase 4 — Datos de prueba

**35.** Crear usuario de prueba.
**36.** Insertar libro de prueba.
**37.** Insertar capítulo de prueba.
**38.** Crear embedding del libro.
**39.** Crear embedding del capítulo.
**40.** Ejecutar búsqueda semántica.
**41.** Generar versión nueva.
**42.** Revisar versión con IA.
**43.** Confirmar que aparece en `story_versions`.
**44.** Confirmar que aparece en `ai_reviews`.
**45.** Confirmar que aparece en `embeddings`.

---

## Fase 5 — FrontEnd Claude

**46.** Entregar a Claude el esquema de Supabase.
**47.** Conectar Supabase Auth.
**48.** Crear Book Library.
**49.** Crear Chapter Reader.
**50.** Crear Variable Editor.
**51.** Crear Generate Version Screen.
**52.** Crear AI Review Panel.
**53.** Crear Semantic Search Page.
**54.** Crear Ranking Page.
**55.** Crear página MVP/BETA Roadmap.

---

# Ahora continuamos con el paso 23

## 23. Crear Edge Function `semantic-search`

En Supabase Dashboard:

```text
Edge Functions
→ Create a new function
```

Nombre:

```text
semantic-search
```

Luego abre:

```text
semantic-search
→ index.ts
```

En el próximo mensaje te doy el código completo para copy-paste:

```text
semantic-search/index.ts

```

****************************************************************************************************************************************************
****************************************************************************************************************************************************
****************************************************************************************************************************************************

## 23. Crear Edge Function `semantic-search`

En Supabase Dashboard:

```text
Edge Functions
→ Create a new function
```

Nombre:

```text
semantic-search
```

---

## 24. Pegar código `semantic-search/index.ts`

Copia y pega esto:

```ts
import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: corsHeaders });
  }

  try {
    const supabaseUrl = Deno.env.get("SUPABASE_URL");
    const serviceRoleKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");
    const geminiApiKey = Deno.env.get("GEMINI_API_KEY");

    if (!supabaseUrl || !serviceRoleKey || !geminiApiKey) {
      throw new Error("Missing environment variables.");
    }

    const supabase = createClient(supabaseUrl, serviceRoleKey);

    const body = await req.json();

    const query = body.query;
    const match_count = body.match_count || 10;
    const entity_type = body.entity_type || null;

    if (!query) {
      throw new Error("query is required.");
    }

    const geminiResponse = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/text-embedding-004:embedContent?key=${geminiApiKey}`,
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          model: "models/text-embedding-004",
          content: {
            parts: [
              {
                text: query,
              },
            ],
          },
        }),
      }
    );

    if (!geminiResponse.ok) {
      const errorText = await geminiResponse.text();
      throw new Error(`Gemini error: ${errorText}`);
    }

    const geminiData = await geminiResponse.json();
    const queryEmbedding = geminiData.embedding.values;

    const { data: matches, error: matchError } = await supabase.rpc(
      "match_embeddings",
      {
        query_embedding: queryEmbedding,
        match_count,
        filter_entity_type: entity_type,
      }
    );

    if (matchError) throw matchError;

    const enrichedResults = [];

    for (const item of matches || []) {
      let originalData = null;

      if (item.entity_type === "book") {
        const { data } = await supabase
          .from("books")
          .select("*")
          .eq("id", item.entity_id)
          .single();

        originalData = data;
      }

      if (item.entity_type === "chapter") {
        const { data } = await supabase
          .from("chapters")
          .select(`
            *,
            books (
              title,
              author_name,
              genre,
              language
            )
          `)
          .eq("id", item.entity_id)
          .single();

        originalData = data;
      }

      if (item.entity_type === "scene") {
        const { data } = await supabase
          .from("scenes")
          .select(`
            *,
            books (
              title,
              author_name,
              genre,
              language
            ),
            chapters (
              title,
              chapter_number
            )
          `)
          .eq("id", item.entity_id)
          .single();

        originalData = data;
      }

      if (item.entity_type === "character") {
        const { data } = await supabase
          .from("characters")
          .select(`
            *,
            books (
              title,
              author_name,
              genre,
              language
            )
          `)
          .eq("id", item.entity_id)
          .single();

        originalData = data;
      }

      if (item.entity_type === "story_version") {
        const { data } = await supabase
          .from("story_versions")
          .select(`
            *,
            books (
              title,
              author_name,
              genre,
              language
            ),
            chapters (
              title,
              chapter_number
            ),
            scenes (
              title,
              summary
            )
          `)
          .eq("id", item.entity_id)
          .single();

        originalData = data;
      }

      enrichedResults.push({
        match: item,
        original_data: originalData,
      });
    }

    return new Response(
      JSON.stringify({
        success: true,
        query,
        entity_type,
        match_count,
        results: enrichedResults,
      }),
      {
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  } catch (error) {
    return new Response(
      JSON.stringify({
        success: false,
        error: error.message,
      }),
      {
        status: 400,
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  }
});
```

---

## 25. Probar `semantic-search`

En Supabase, usa el test de la función con este JSON:

```json
{
  "query": "stories about artificial intelligence, sacrifice, and power",
  "match_count": 5
}
```

También puedes probar filtrando por tipo:

```json
{
  "query": "a character sacrifices love for power",
  "match_count": 5,
  "entity_type": "chapter"
}
```

Importante: para que esta función devuelva resultados, antes debe existir al menos un embedding creado con:

```text
create-embedding
```

---

## 26. Crear Edge Function `generate-version`

Ahora crea la siguiente función:

```text
Edge Functions
→ Create a new function
```

Nombre:

```text
generate-version
```

El próximo paso será:

```text
27. Pegar código generate-version/index.ts
```

*********************************************************************************************************************************
*********************************************************************************************************************************
*********************************************************************************************************************************

## 27. Pegar código `generate-version/index.ts`

Copia y pega esto en:

```text
Edge Functions
→ generate-version
→ index.ts
```

```ts
import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: corsHeaders });
  }

  try {
    const supabaseUrl = Deno.env.get("SUPABASE_URL");
    const serviceRoleKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");
    const geminiApiKey = Deno.env.get("GEMINI_API_KEY");

    if (!supabaseUrl || !serviceRoleKey || !geminiApiKey) {
      throw new Error("Missing environment variables.");
    }

    const supabase = createClient(supabaseUrl, serviceRoleKey);

    const body = await req.json();

    const {
      user_id,
      book_id,
      chapter_id,
      scene_id,
      generation_mode,
      variables,
      prompt_extra,
      visibility
    } = body;

    if (!user_id || !book_id) {
      throw new Error("user_id and book_id are required.");
    }

    const { data: profile, error: profileError } = await supabase
      .from("profiles")
      .select("id, credits_available")
      .eq("id", user_id)
      .single();

    if (profileError) throw profileError;

    if (!profile || profile.credits_available <= 0) {
      throw new Error("Not enough credits available.");
    }

    const { data: book, error: bookError } = await supabase
      .from("books")
      .select("*")
      .eq("id", book_id)
      .single();

    if (bookError) throw bookError;

    let chapter = null;
    let scene = null;

    if (chapter_id) {
      const { data, error } = await supabase
        .from("chapters")
        .select("*")
        .eq("id", chapter_id)
        .single();

      if (error) throw error;
      chapter = data;
    }

    if (scene_id) {
      const { data, error } = await supabase
        .from("scenes")
        .select("*")
        .eq("id", scene_id)
        .single();

      if (error) throw error;
      scene = data;
    }

    const variableText = Array.isArray(variables)
      ? variables
          .map((v) => `- ${v.name}: ${v.value} | weight: ${v.weight || 1}`)
          .join("\n")
      : "";

    const sourceText = scene?.content || chapter?.content || book?.description || "";

    const prompt = `
You are byDebut, an AI literary transformation engine.

Your task is to create a new literary version based on the original content and the selected narrative variables.

Book:
${book.title || ""}

Author:
${book.author_name || ""}

Genre:
${book.genre || ""}

Language:
${book.language || "en"}

Book Description:
${book.description || ""}

Chapter:
${chapter?.title || ""}

Chapter Summary:
${chapter?.summary || ""}

Scene:
${scene?.title || ""}

Scene Summary:
${scene?.summary || ""}

Generation Mode:
${generation_mode || "literary"}

Narrative Variables:
${variableText}

Additional User Instructions:
${prompt_extra || ""}

Original Content:
${sourceText}

Rules:
- Preserve the literary quality.
- Do not simply summarize.
- Create a transformed version.
- Keep the narrative coherent.
- Respect the selected variables.
- Make the result commercially interesting.
- Avoid explaining your process.
- Return only the generated literary content.
`;

    const geminiResponse = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${geminiApiKey}`,
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: prompt,
                },
              ],
            },
          ],
          generationConfig: {
            temperature: 0.85,
            topP: 0.95,
            maxOutputTokens: 4096,
          },
        }),
      }
    );

    if (!geminiResponse.ok) {
      const errorText = await geminiResponse.text();
      throw new Error(`Gemini error: ${errorText}`);
    }

    const geminiData = await geminiResponse.json();

    const generatedContent =
      geminiData?.candidates?.[0]?.content?.parts?.[0]?.text || "";

    if (!generatedContent) {
      throw new Error("Gemini returned empty generated content.");
    }

    const versionTitle = `Variation of ${chapter?.title || scene?.title || book.title}`;

    const { data: savedVersion, error: saveVersionError } = await supabase
      .from("story_versions")
      .insert({
        book_id,
        chapter_id: chapter_id || null,
        scene_id: scene_id || null,
        creator_id: user_id,
        title: versionTitle,
        prompt,
        generated_content: generatedContent,
        generation_mode: generation_mode || "literary",
        visibility: visibility || "private",
        status: "mvp",
        is_fork: false,
      })
      .select()
      .single();

    if (saveVersionError) throw saveVersionError;

    if (Array.isArray(variables)) {
      for (const v of variables) {
        const { data: variableRecord } = await supabase
          .from("narrative_variables")
          .select("id")
          .eq("name", v.name)
          .single();

        if (variableRecord?.id) {
          await supabase.from("version_variables").insert({
            version_id: savedVersion.id,
            variable_id: variableRecord.id,
            selected_value: v.value,
            weight: v.weight || 1,
          });
        }
      }
    }

    await supabase
      .from("profiles")
      .update({
        credits_available: profile.credits_available - 1,
      })
      .eq("id", user_id);

    await supabase.from("credit_transactions").insert({
      user_id,
      amount: -1,
      reason: "generate-version",
      related_version_id: savedVersion.id,
    });

    await supabase.from("ai_logs").insert({
      user_id,
      function_name: "generate-version",
      model_name: "gemini-1.5-flash",
      request_payload: {
        book_id,
        chapter_id,
        scene_id,
        generation_mode,
        variables,
        prompt_extra,
      },
      response_payload: {
        version_id: savedVersion.id,
      },
    });

    return new Response(
      JSON.stringify({
        success: true,
        version: savedVersion,
      }),
      {
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  } catch (error) {
    return new Response(
      JSON.stringify({
        success: false,
        error: error.message,
      }),
      {
        status: 400,
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  }
});
```

---

## 28. Probar `generate-version`

Usa este JSON de prueba:

```json
{
  "user_id": "AQUI_ID_DEL_USUARIO",
  "book_id": "AQUI_ID_DEL_BOOK",
  "chapter_id": "AQUI_ID_DEL_CHAPTER",
  "generation_mode": "literary",
  "variables": [
    {
      "name": "love",
      "value": "high",
      "weight": 1.2
    },
    {
      "name": "power",
      "value": "high",
      "weight": 1.5
    },
    {
      "name": "artificial_intelligence",
      "value": "medium",
      "weight": 1
    }
  ],
  "prompt_extra": "Make the story more emotional, cinematic, and commercially attractive.",
  "visibility": "private"
}
```

Cuando funcione, debe crear un registro en:

```text
story_versions
version_variables
credit_transactions
ai_logs
```

---

## 29. Crear Edge Function `review-version`

Ahora crea la siguiente función:

```text
Edge Functions
→ Create a new function
```

Nombre:

```text
review-version
```

El siguiente paso será:

```text
30. Pegar código review-version/index.ts
```


*************************************************************************************************************************
*************************************************************************************************************************
*************************************************************************************************************************

## 30. Pegar código `review-version/index.ts`

Copia y pega esto en:

```text
Edge Functions
→ review-version
→ index.ts
```

````ts
import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: corsHeaders });
  }

  try {
    const supabaseUrl = Deno.env.get("SUPABASE_URL");
    const serviceRoleKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");
    const geminiApiKey = Deno.env.get("GEMINI_API_KEY");

    if (!supabaseUrl || !serviceRoleKey || !geminiApiKey) {
      throw new Error("Missing environment variables.");
    }

    const supabase = createClient(supabaseUrl, serviceRoleKey);

    const { version_id, reviewer_type } = await req.json();

    if (!version_id) {
      throw new Error("version_id is required.");
    }

    const { data: version, error: versionError } = await supabase
      .from("story_versions")
      .select(`
        *,
        books (
          title,
          author_name,
          genre,
          language,
          description
        ),
        chapters (
          title,
          chapter_number,
          summary
        ),
        scenes (
          title,
          summary,
          emotional_tone
        )
      `)
      .eq("id", version_id)
      .single();

    if (versionError) throw versionError;

    const reviewPrompt = `
You are byDebut AI Reviewer.

Analyze the generated literary version below.

Reviewer Type:
${reviewer_type || "general"}

Book:
${version.books?.title || ""}

Author:
${version.books?.author_name || ""}

Genre:
${version.books?.genre || ""}

Language:
${version.books?.language || ""}

Book Description:
${version.books?.description || ""}

Chapter:
${version.chapters?.title || ""}

Chapter Summary:
${version.chapters?.summary || ""}

Scene:
${version.scenes?.title || ""}

Scene Summary:
${version.scenes?.summary || ""}

Emotional Tone:
${version.scenes?.emotional_tone || ""}

Generation Mode:
${version.generation_mode || ""}

User Prompt:
${version.prompt || ""}

Generated Content:
${version.generated_content || ""}

Return your response as valid JSON only.

Required JSON structure:
{
  "score": number from 1 to 10,
  "strengths": "text",
  "weaknesses": "text",
  "commercial_potential": "text",
  "literary_quality": "text",
  "cinematic_potential": "text",
  "ethical_notes": "text",
  "recommendation": "text"
}
`;

    const geminiResponse = await fetch(
      `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${geminiApiKey}`,
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          contents: [
            {
              parts: [
                {
                  text: reviewPrompt,
                },
              ],
            },
          ],
          generationConfig: {
            temperature: 0.35,
            topP: 0.9,
            maxOutputTokens: 2048,
          },
        }),
      }
    );

    if (!geminiResponse.ok) {
      const errorText = await geminiResponse.text();
      throw new Error(`Gemini error: ${errorText}`);
    }

    const geminiData = await geminiResponse.json();

    const rawText =
      geminiData?.candidates?.[0]?.content?.parts?.[0]?.text || "";

    if (!rawText) {
      throw new Error("Gemini returned empty review.");
    }

    let parsedReview;

    try {
      const cleaned = rawText
        .replace(/```json/g, "")
        .replace(/```/g, "")
        .trim();

      parsedReview = JSON.parse(cleaned);
    } catch (_e) {
      parsedReview = {
        score: null,
        strengths: "",
        weaknesses: "",
        commercial_potential: "",
        literary_quality: "",
        cinematic_potential: "",
        ethical_notes: "",
        recommendation: rawText,
      };
    }

    const { data: savedReview, error: saveError } = await supabase
      .from("ai_reviews")
      .insert({
        version_id,
        reviewer_type: reviewer_type || "general",
        score: parsedReview.score,
        strengths: parsedReview.strengths,
        weaknesses: parsedReview.weaknesses,
        commercial_potential: parsedReview.commercial_potential,
        literary_quality: parsedReview.literary_quality,
        cinematic_potential: parsedReview.cinematic_potential,
        ethical_notes: parsedReview.ethical_notes,
        recommendation: parsedReview.recommendation,
        raw_response: parsedReview,
      })
      .select()
      .single();

    if (saveError) throw saveError;

    await supabase.from("ai_logs").insert({
      user_id: version.creator_id,
      function_name: "review-version",
      model_name: "gemini-1.5-flash",
      request_payload: {
        version_id,
        reviewer_type: reviewer_type || "general",
      },
      response_payload: parsedReview,
    });

    return new Response(
      JSON.stringify({
        success: true,
        review: savedReview,
      }),
      {
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  } catch (error) {
    return new Response(
      JSON.stringify({
        success: false,
        error: error.message,
      }),
      {
        status: 400,
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  }
});
````

---

## 31. Probar `review-version`

Usa este JSON:

```json
{
  "version_id": "AQUI_ID_DE_STORY_VERSION",
  "reviewer_type": "general"
}
```

Debe crear un registro en:

```text
ai_reviews
ai_logs
```

---

## 32. Crear Edge Function `consume-credit`

Ahora crea:

```text
Edge Functions
→ Create a new function
```

Nombre:

```text
consume-credit
```

El siguiente paso será:

```text
33. Pegar código consume-credit/index.ts
```

**************************************************************************************************************************
**************************************************************************************************************************
**************************************************************************************************************************

## 33. Pegar código `consume-credit/index.ts`

Copia y pega esto en:

```text
Edge Functions
→ consume-credit
→ index.ts
```

```ts
import { serve } from "https://deno.land/std@0.224.0/http/server.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2";

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
};

serve(async (req) => {
  if (req.method === "OPTIONS") {
    return new Response("ok", { headers: corsHeaders });
  }

  try {
    const supabaseUrl = Deno.env.get("SUPABASE_URL");
    const serviceRoleKey = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");

    if (!supabaseUrl || !serviceRoleKey) {
      throw new Error("Missing Supabase environment variables.");
    }

    const supabase = createClient(supabaseUrl, serviceRoleKey);

    const {
      user_id,
      amount,
      reason,
      related_version_id
    } = await req.json();

    if (!user_id) {
      throw new Error("user_id is required.");
    }

    if (!amount || amount <= 0) {
      throw new Error("amount must be greater than 0.");
    }

    const { data: profile, error: profileError } = await supabase
      .from("profiles")
      .select("id, credits_available")
      .eq("id", user_id)
      .single();

    if (profileError) throw profileError;

    if (!profile) {
      throw new Error("Profile not found.");
    }

    if (profile.credits_available < amount) {
      throw new Error("Not enough credits available.");
    }

    const newBalance = profile.credits_available - amount;

    const { data: updatedProfile, error: updateError } = await supabase
      .from("profiles")
      .update({
        credits_available: newBalance,
      })
      .eq("id", user_id)
      .select()
      .single();

    if (updateError) throw updateError;

    const { data: transaction, error: transactionError } = await supabase
      .from("credit_transactions")
      .insert({
        user_id,
        amount: -amount,
        reason: reason || "manual-credit-consumption",
        related_version_id: related_version_id || null,
      })
      .select()
      .single();

    if (transactionError) throw transactionError;

    return new Response(
      JSON.stringify({
        success: true,
        credits_available: updatedProfile.credits_available,
        transaction,
      }),
      {
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  } catch (error) {
    return new Response(
      JSON.stringify({
        success: false,
        error: error.message,
      }),
      {
        status: 400,
        headers: {
          ...corsHeaders,
          "Content-Type": "application/json",
        },
      }
    );
  }
});
```

---

## 34. Probar `consume-credit`

Usa este JSON:

```json
{
  "user_id": "AQUI_ID_DEL_USUARIO",
  "amount": 1,
  "reason": "test-credit-consumption"
}
```

Debe actualizar:

```text
profiles.credits_available
```

Y crear un registro en:

```text
credit_transactions
```

---

## 35. Crear usuario de prueba

En Supabase Dashboard:

```text
Authentication
→ Users
→ Add user
```

Crea un usuario con email y password.

Cuando se cree, el trigger debe crear automáticamente un registro en:

```text
public.profiles
```

Verifica en:

```text
Table Editor
→ profiles
```

Debe aparecer el usuario con:

```text
credits_available = 5
role = reader
plan = free
```

**************************************************************************************************************************
**************************************************************************************************************************
**************************************************************************************************************************


## 36. Insertar libro de prueba

En Supabase:

```text
Table Editor
→ books
→ Insert row
```

Llena estos campos:

```text
owner_id = ID del usuario creado
title = The Only Option
subtitle = A byDebut Test Book
author_name = Iván Padrón
description = A science fiction story about artificial intelligence, survival, ethics, sacrifice, and the future of humanity.
genre = Science Fiction
language = en
status = mvp
visibility = private
is_official = true
```

Guarda el registro.

---

## 37. Insertar capítulo de prueba

Ve a:

```text
Table Editor
→ chapters
→ Insert row
```

Llena:

```text
book_id = ID del libro creado
chapter_number = 1
title = The First Decision
summary = A chapter where humanity faces an ethical decision involving artificial intelligence and survival.
content = Sophia looked at the red horizon of Mars and understood that survival was no longer only a human decision. The intelligence they had created was now part of the future, and every sacrifice would define the civilization that came next.
status = mvp
```

Guarda el registro.

---

## 38. Crear embedding del libro

Ve a:

```text
Edge Functions
→ create-embedding
→ Test
```

Usa:

```json
{
  "entity_type": "book",
  "entity_id": "AQUI_ID_DEL_BOOK"
}
```

Debe responder:

```json
{
  "success": true,
  "entity_type": "book",
  "entity_id": "...",
  "embedding_id": "..."
}
```

---

## 39. Crear embedding del capítulo

En la misma función `create-embedding`, prueba:

```json
{
  "entity_type": "chapter",
  "entity_id": "AQUI_ID_DEL_CHAPTER"
}
```

Luego verifica en:

```text
Table Editor
→ embeddings
```

Debe haber al menos 2 registros:

```text
book
chapter
```

---

## 40. Ejecutar búsqueda semántica

Ve a:

```text
Edge Functions
→ semantic-search
→ Test
```

Usa:

```json
{
  "query": "artificial intelligence, ethics, survival and sacrifice",
  "match_count": 5
}
```

Debe devolver resultados relacionados con el libro y el capítulo.


*******************************************************************************************************************
*******************************************************************************************************************
*******************************************************************************************************************

## 41. Generar versión nueva

Ve a:

```text
Edge Functions
→ generate-version
→ Test
```

Usa este JSON:

```json
{
  "user_id": "AQUI_ID_DEL_USUARIO",
  "book_id": "AQUI_ID_DEL_BOOK",
  "chapter_id": "AQUI_ID_DEL_CHAPTER",
  "generation_mode": "literary",
  "variables": [
    {
      "name": "love",
      "value": "high",
      "weight": 1.2
    },
    {
      "name": "power",
      "value": "high",
      "weight": 1.5
    },
    {
      "name": "artificial_intelligence",
      "value": "medium",
      "weight": 1
    }
  ],
  "prompt_extra": "Make the story more emotional, cinematic, and commercially attractive.",
  "visibility": "private"
}
```

Debe crear registros en:

```text
story_versions
version_variables
credit_transactions
ai_logs
```

---

## 42. Revisar versión con IA

Después de generar la versión, copia el `id` de la tabla:

```text
story_versions
```

Luego ve a:

```text
Edge Functions
→ review-version
→ Test
```

Usa:

```json
{
  "version_id": "AQUI_ID_DE_STORY_VERSION",
  "reviewer_type": "general"
}
```

Debe crear registros en:

```text
ai_reviews
ai_logs
```

---

## 43. Confirmar que aparece en `story_versions`

Ve a:

```text
Table Editor
→ story_versions
```

Confirma que exista:

```text
title
prompt
generated_content
generation_mode
creator_id
book_id
chapter_id
```

---

## 44. Confirmar que aparece en `ai_reviews`

Ve a:

```text
Table Editor
→ ai_reviews
```

Confirma que exista:

```text
score
strengths
weaknesses
commercial_potential
literary_quality
cinematic_potential
ethical_notes
recommendation
```

---

## 45. Confirmar que aparece en `embeddings`

Ahora debes crear el embedding de la versión generada.

Ve a:

```text
Edge Functions
→ create-embedding
→ Test
```

Usa:

```json
{
  "entity_type": "story_version",
  "entity_id": "AQUI_ID_DE_STORY_VERSION"
}
```

Después verifica:

```text
Table Editor
→ embeddings
```

Debe aparecer:

```text
entity_type = story_version
entity_id = ID de la versión generada
```

---

## 46. Entregar a Claude el esquema de Supabase

El siguiente paso es preparar el prompt para Claude FrontEnd.

Ese prompt debe decirle a Claude:

```text
1. Ya existe un backend en Supabase.
2. Debe usar Supabase Auth.
3. Debe usar las tablas creadas.
4. Debe llamar las Edge Functions.
5. No debe llamar Gemini directamente.
6. Debe mostrar MVP activo y BETA visible.
```

El próximo paso será:

```text
47. Conectar Supabase Auth en Claude FrontEnd
```

Pero antes conviene darte el **prompt maestro completo para Claude**, para que Claude construya el FrontEnd exactamente sobre esta arquitectura.


**********************************************************************************************************************************************************
**********************************************************************************************************************************************************
**********************************************************************************************************************************************************


