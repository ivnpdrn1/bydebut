Aquí tienes el texto completo, unificado y listo para **copiar y pegar**. He mantenido la redacción original y he corregido únicamente pequeños errores de lectura producidos por la fotografía.

````text
Crea una app SaaS llamada Daily Photo Journal.

Es un diario privado donde cada usuario puede crear una entrada diaria con 1 foto y 1 texto, y solo él puede verlas.

Diseño limpio, moderno y mobile-first.

El backend debe estar en Supabase, con lo que puedes conectar el frontend que diseñes con el proyecto "DiarioPersonal" que encontrarás en Supabase.

Te paso las credenciales de Supabase:

PROJECT_URL =
https://nyyrvjgymioowicmrkfx.supabase.co

https://nyyrvjgymioowicmrkfx.supabase.co/

Publishable API_KEY =
sb_publishable_-YTnCJ7b-nTSJZ7zN9EoFw_SomRJofF*

En Supabase tienes una tabla llamada "entries" que almacenará el texto que el usuario escriba junto a la foto.

Y luego tendrás un bucket llamado "journal-images" en el que cada usuario tendrá una carpeta con su ID, con lo que cuando un usuario suba una imagen esa imagen se almacenará en su carpeta y así solo la podrá ver él en su área privada.

En cuanto a la tabla con el texto que subirá cada usuario junto a la foto, aquí tienes el esquema de la tabla:

```sql
create table public.entries (
    id bigint generated by default as identity not null,
    created_at timestamp with time zone not null default now(),
    entry_date date null,
    text text null,
    image_path text null,
    updated_at timestamp with time zone null default now(),
    user_id uuid null,

    constraint entries_pkey primary key (id),
    constraint entries_user_id_fkey
        foreign key (user_id)
        references auth.users (id)
) TABLESPACE pg_default;
```
````

## Hay un detalle importante

La **Publishable API Key NO está completa en la fotografía**. La imagen corta la clave y solo alcanza a verse:

```text
sb_publishable_-YTnCJ7b-nTSJZ7zN9EoFw_SomRJofF*
```

Una **Publishable Key de Supabase normalmente tiene más de 200 caracteres**, por lo que la captura no contiene la clave completa.

---

Además, veo que en la última imagen aparece **otro proyecto distinto**, con otras credenciales:

```
PROJECT_URL
https://dhltfphvacmzzowfbxgb.supabase.co

Publishable API_KEY
sb_publishable_WeH...
```

## Si tu intención es construir este proyecto con Claude o Lovable, puedo prepararte un **prompt profesional mucho mejor que éste**, incluyendo:

* arquitectura completa,
* autenticación,
* Row Level Security (RLS),
* Storage Policies,
* UI moderna,
* calendario,
* subida de imágenes,
* búsqueda,
* responsive,
* y código listo para producción.

Quedaría con calidad de un **PRD profesional**, no simplemente una descripción del proyecto.

